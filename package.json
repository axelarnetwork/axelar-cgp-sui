{
  "name": "@axelar-network/axelar-cgp-sui",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/axelarnetwork/axelar-cgp-sui"
  },
  "main": "index.js",  
  "scripts": {
    "build": "cd move/axelar && sui move build && cd ../test && sui move build",
    "prepare": "node scripts/publish-package axelar && node scripts/publish-package test && node scripts/its/discovery && node scripts/its/set-trusted-address"
  },
  "keywords": [
    "axelar",
    "sui"
  ],
  "author": "axelar-network",
  "license": "MIT",
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "@mysten/sui.js": "^0.47.0",
    "child_process": "^1.0.2",
    "dotenv": "^16.3.1",
    "ethers": "^5.0.0",
    "fs": "^0.0.1-security",
    "secp256k1": "^5.0.0",
    "tmp": "^0.2.1"
  },
  "description": ""
}
